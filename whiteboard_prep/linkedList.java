//LinkedList
//Implimented with a doubly-linked list, while ArrayList impliments with a dynamically re-sizing array.
//In a LinkedList, each element points to the element that comes after it.
LinkedList<String> foods = new LinkedList<String>();
foods.add("Burgers");
foods.add("Pizza");
foods.add("Cookies");
foods.get(1); //"Pizza"
foods.set(2, "Pie"
